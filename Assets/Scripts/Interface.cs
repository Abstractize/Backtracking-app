using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Backtracking;
using GenMatrix;

public class Interface : MonoBehaviour {
    int level;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
    /*
     * Obtiene la matriz
     * Grafica la matriz
     * Obtiene el Backtracking
     * Paso por paso lo refleja
     * Cuando el Backtracking le avisa, termina
     * Aumenta el nivel
     * Inicia de nuevo
     * cuando el lvl es 9 se cierra
     */
}
